% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{nrouter_multipart}
\alias{nrouter_multipart}
\title{Send a multipart request to the nRouter gateway}
\usage{
nrouter_multipart(client, path, file_path, fields = list())
}
\arguments{
\item{client}{An \code{nrouter_client}.}

\item{path}{Path under the gateway's \code{/v1} root.}

\item{file_path}{Path to the file to upload. Its EXTENSION is load-bearing —
the upstream providers pick their decoder from it.}

\item{fields}{Named list of additional form fields, e.g. \code{model}.}
}
\value{
A list with \code{body}, \code{meta} and \code{status_code}.
}
\description{
multipart/form-data, not JSON: \code{/v1/audio/transcriptions} and
\code{/v1/audio/translations} require a binary \code{file} part, so the JSON
helpers cannot reach those endpoints at all.
}
