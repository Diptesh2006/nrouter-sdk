% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{nrouter_request}
\alias{nrouter_request}
\title{Send a request to the nRouter gateway}
\usage{
nrouter_request(
  client,
  path,
  body = NULL,
  method = if (is.null(body)) "GET" else "POST"
)
}
\arguments{
\item{client}{An \code{nrouter_client}.}

\item{path}{Path under the gateway's \code{/v1} root, e.g. \code{"/models"}.}

\item{body}{Named list to send as JSON, or \code{NULL} for a GET.}

\item{method}{HTTP method; inferred from \code{body} when not given.}
}
\value{
A list with \code{body}, \code{meta} and \code{status_code}.
}
\description{
Send a request to the nRouter gateway
}
